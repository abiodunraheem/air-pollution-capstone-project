import { SEARCH_COUNTRY, SELECT_COUNTRY, CANCEL_COUNTRY } from './actionTypes';

const initialState = [];

// reducer function
const countryReducer = (state = initialState, action) => {
    // switch (action.type) {
    //     case SEARCH_COUNTRY:
    //         return payload.action
    //     case SELECT_COUNTRY:
    //         return payload.action
    //     case CANCEL_COUNTRY:
    //         return payload.action  
    //     default:
    //         return state;
    // }
}

export default countryReducer;